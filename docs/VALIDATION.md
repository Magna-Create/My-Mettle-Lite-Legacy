# Phase 1 Validation Record

Validated on 19 July 2026.

## Passed

- `npm test`: 10 tests passed across cycle rules, performance rules and application workflows.
- `npm run build`: TypeScript strict build and Vite production bundle passed.
- `npm run android:sync`: production web assets copied into the Capacitor Android shell and plugins synchronised.
- The built application is served successfully through Vite preview.

## Test coverage in this slice

- ψ is recommended at the beginning of a clean cycle.
- & remains locked until ψ, φ and π are complete.
- A new cycle opens when a core day begins after the previous three core days are complete.
- Deterministic work-volume calculation.
- The retained >3 same-load repetition-drop warning.
- Session creation and set persistence.
- Manual exercise creation produces a new immutable routine version.
- Successful work produces a micro-load proposal.
- An active experiment changes only its exact routine slot temporarily.
- A tested experiment requires explicit promotion into a new routine version.

## Environment limitation

The generated Android project could not be assembled into an APK inside this container because Gradle's distribution server is unavailable from the execution network. The Capacitor project itself generated and synchronised successfully. Android Studio or a networked local Gradle environment is required for the first APK/device build.
